---
'@x402/mcp': patch
---

Added `monetization-mcp-server` example: a self-contained MCP server over SSE that gates tool calls with x402 payments, demonstrating `createPaymentWrapper` for Solana MEV and Apify scraping tools
