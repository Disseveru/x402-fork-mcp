interface:
  display_name: "X402 Fork Mcp"
  short_description: "Repo-specific patterns and workflows for x402-fork-mcp"
  default_prompt: "Use the x402-fork-mcp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true